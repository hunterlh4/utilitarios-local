// Juegos de Steam (SteamItem table)
export enum SteamGame {
  Dota2 = 1,
  CS2 = 2,
}

// Estado de items de Steam (SteamItem table)
export enum SteamItemStatus {
  Historial = 1,
  PorComprar = 2,
}

// Estado de compras de Steam (SteamItemPurchase table)
export enum SteamPurchaseStatus {
  Comprado = 1,
  Vendido = 2,
}

// Tipo de tesoro de Dota (DotaTreasure table)
export enum DotaTreasureType {
  TreasureI = 1,
  TreasureII = 2,
}
