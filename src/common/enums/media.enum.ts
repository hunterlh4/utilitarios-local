// Tipos de media (Media table)
export enum MediaType {
  GirlGalery = 1,
  AnimeGalery = 2,
  Project = 3,
}

// Tipos de media para Dota (DotaMedia table)
export enum DotaMediaType {
  DotaTreasure = 1,
  DotaCache = 2,
}
